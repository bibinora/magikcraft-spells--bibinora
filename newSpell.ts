const magik = magikcraft.io;
function zombieA(){
    const sender = magik.getSender();
    const EntiyType = magik.type("entity.EntityType");
    const Zombie = EntiyType['ZOMBIE'];

    for(var i = 0; i <1000; i++){
        var arrow = sander.getWorld().spawnEntity(magik.aspecto())
    }
